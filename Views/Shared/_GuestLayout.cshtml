@using Microsoft.AspNetCore.Http
@inject IHttpContextAccessor HttpContextAccessor
<!DOCTYPE html>
<html>
<head>
    <title>@ViewData["Title"] - Eventalk</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css" rel="stylesheet">
    <style>
        body {
            margin: 0;
            font-family: 'Segoe UI', sans-serif;
        }

        .navbar-brand span {
            color: #e8450a;
        }

        .nav-link {
            color: #333 !important;
            font-weight: 500;
        }

            .nav-link:hover {
                color: #e8450a !important;
            }

            .nav-link.active-link {
                color: #e8450a !important;
                font-weight: 600;
            }

        .btn-getstarted {
            background-color: #e8450a;
            color: white;
            border-radius: 6px;
            padding: 8px 20px;
            font-weight: 600;
            border: none;
        }

            .btn-getstarted:hover {
                background-color: #c93a08;
                color: white;
            }

        footer {
            background: #0b032d;
            color: #ccc;
            padding: 40px 0 20px;
        }

            footer a {
                color: #ccc;
                text-decoration: none;
                display: block;
                margin-bottom: 6px;
            }

                footer a:hover {
                    color: #e8450a;
                }

            footer .brand {
                color: white;
                font-weight: 700;
                font-size: 1.2rem;
            }

                footer .brand span {
                    color: #e8450a;
                }
    </style>
</head>
<body>

    <!-- NAVBAR -->
    <nav class="navbar navbar-expand-lg bg-white border-bottom px-4 py-3 sticky-top shadow-sm">
        <a class="navbar-brand fw-bold fs-4" href="/Home/Index">
            Event<span>talk.</span>
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#guestNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="guestNav">
            <ul class="navbar-nav mx-auto gap-3">
                <li class="nav-item">
                    <a class="nav-link @(Context.Request.Path == "/Home/Index" || Context.Request.Path == "/" ? "active-link" : "")"
                       href="/Home/Index">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link @(Context.Request.Path.StartsWithSegments("/Event") ? "active-link" : "")"
                       href="/Event/Index">Events</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link @(Context.Request.Path.StartsWithSegments("/Home/About") ? "active-link" : "")"
                       href="/Home/About">About</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link @(Context.Request.Path.StartsWithSegments("/Home/Contact") ? "active-link" : "")"
                       href="/Home/Contact">Contact</a>
                </li>
            </ul>
            <div class="d-flex gap-2 align-items-center">
                <a href="/Account/Login" class="nav-link fw-semibold">Login</a>
                <a href="/Account/Register" class="btn btn-getstarted">Get Started</a>
            </div>
        </div>
    </nav>

    <!-- PAGE CONTENT -->
    @RenderBody()

    <!-- FOOTER -->
    <footer>
        <div class="container">
            <div class="row">
                <div class="col-md-6 mb-4">
                    <div class="brand mb-2">Event<span>talk.</span></div>
                    <p style="font-size:0.9rem;">A web-based platform for organizing and managing events efficiently.</p>
                </div>
                <div class="col-md-3 mb-4">
                    <h6 class="text-white fw-bold mb-3">Quick Links</h6>
                    <a href="/Home/Index">Home</a>
                    <a href="/Event/Index">Events</a>
                    <a href="/Home/About">About Us</a>
                    <a href="/Home/Contact">Contact Us</a>
                </div>
            </div>
            <hr style="border-color:#1a0a4f;">
            <p class="text-center mb-0" style="font-size:0.85rem;">
                &copy; @DateTime.Now.Year Eventalk. All rights reserved.
            </p>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    @RenderSection("Scripts", required: false)
</body>
</html>